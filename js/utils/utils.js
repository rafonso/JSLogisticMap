/*jshint -W079 */
/**
 * @file Contains constants and utilities to be used by both view side and worker as well.
 */
"use strict";

/**
 * activates debubbing.
 * 
 * @const
 * @type {boolean}
 * @default
 */
var DEBUG = true;

/**
 * Minimum value for x0
 * @const {number} 
 * @default
 */
const X_MIN = 0.0;

/**
 * Maximum value for x0
 * @const {number} 
 * @default
 */
const X_MAX = 1.0;

/**
 * Minimum value for r
 * @const {number} 
 * @default
 */
const R_MIN = 0.0;

/**
 * Maximum value for r
 * @const {number} 
 * @default
 */
const R_MAX = 4.0;
